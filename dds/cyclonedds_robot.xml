<CycloneDDS>
  <Domain>
    <General>
      <AllowMulticast>false</AllowMulticast>
      <Interfaces>
        <NetworkInterface address="${ROBOT_IP}"/>
        <!-- Allows nodes to communicate in the same host (robot) -->
        <NetworkInterface name="lo"/>  
      </Interfaces>
    </General>
    <Discovery>
      <ParticipantIndex>auto</ParticipantIndex>
      <Peers>
        <Peer address="${DEV_IP}"/>
        <!-- Allows nodes to communicate in the same host (robot) -->
        <Peer address="127.0.0.1"/>  
      </Peers>
    </Discovery>
  </Domain>
</CycloneDDS>